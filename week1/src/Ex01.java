public class Ex01 {
    public static void main(String[] args) {
        // 이름 생성 규칙
        int num; // 영문자 사용 가능 (소문자)
        int Num; // 영문자 사용 가능 (대문자 포함, 소문자)
        int num123; // 숫자 + 영문자 구성 가능
        int n_um_1_23; // 숫자 + 영문자 + 언더스코어 구성 가능
        int n$um_1$23; // 숫자 + 영문자 + 언더스코어 + $ 구성 가능

        // 숫자로 시작 불가능
        // int 3num;

        // 공백 포함 불가능
        // int 3 num;

        // 미리 정의된 키워드 사용 불가능
        // int case;
        // int break;
        // int final;
        // 등등
    }
}
